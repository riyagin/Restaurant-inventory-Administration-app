# Dependencies
node_modules/
**/node_modules/

# Build output
client/dist/
client/dist-ssr/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Environment / secrets
.env
.env.*
!.env.example
server/config.json
# PM2 config holds the prod DB password + JWT secret — keep the real file
# untracked; only ecosystem.config.example.cjs (placeholders) is committed.
deploy/ecosystem.config.cjs

# Uploaded files (server)
server/uploads/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local-only assets (per-developer)
client/public/favicon.local.*

# Production config — contains server IP, must not be overwritten by git pull
client/public/config.json
