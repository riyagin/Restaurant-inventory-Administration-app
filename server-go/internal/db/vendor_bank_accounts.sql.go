// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: vendor_bank_accounts.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const clearVendorPrimaryBankAccount = `-- name: ClearVendorPrimaryBankAccount :exec
UPDATE vendor_bank_accounts SET is_primary = false
WHERE vendor_id = $1 AND is_primary
`

func (q *Queries) ClearVendorPrimaryBankAccount(ctx context.Context, vendorID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, clearVendorPrimaryBankAccount, vendorID)
	return err
}

const createVendorBankAccount = `-- name: CreateVendorBankAccount :one
INSERT INTO vendor_bank_accounts (
    id, vendor_id, bank_name, account_number, account_holder, bank_branch, is_primary, note
)
VALUES (gen_random_uuid(), $1, $2, $3, $4, $5, $6, $7)
RETURNING id, vendor_id, bank_name, account_number, account_holder, bank_branch,
          is_primary, note, created_at
`

type CreateVendorBankAccountParams struct {
	VendorID      pgtype.UUID `json:"vendor_id"`
	BankName      string      `json:"bank_name"`
	AccountNumber string      `json:"account_number"`
	AccountHolder string      `json:"account_holder"`
	BankBranch    string      `json:"bank_branch"`
	IsPrimary     bool        `json:"is_primary"`
	Note          string      `json:"note"`
}

func (q *Queries) CreateVendorBankAccount(ctx context.Context, arg *CreateVendorBankAccountParams) (*VendorBankAccount, error) {
	row := q.db.QueryRow(ctx, createVendorBankAccount,
		arg.VendorID,
		arg.BankName,
		arg.AccountNumber,
		arg.AccountHolder,
		arg.BankBranch,
		arg.IsPrimary,
		arg.Note,
	)
	var i VendorBankAccount
	err := row.Scan(
		&i.ID,
		&i.VendorID,
		&i.BankName,
		&i.AccountNumber,
		&i.AccountHolder,
		&i.BankBranch,
		&i.IsPrimary,
		&i.Note,
		&i.CreatedAt,
	)
	return &i, err
}

const deleteVendorBankAccount = `-- name: DeleteVendorBankAccount :exec
DELETE FROM vendor_bank_accounts WHERE id = $1
`

func (q *Queries) DeleteVendorBankAccount(ctx context.Context, id pgtype.UUID) error {
	_, err := q.db.Exec(ctx, deleteVendorBankAccount, id)
	return err
}

const getVendorBankAccountByID = `-- name: GetVendorBankAccountByID :one
SELECT id, vendor_id, bank_name, account_number, account_holder, bank_branch,
       is_primary, note, created_at
FROM vendor_bank_accounts WHERE id = $1
`

func (q *Queries) GetVendorBankAccountByID(ctx context.Context, id pgtype.UUID) (*VendorBankAccount, error) {
	row := q.db.QueryRow(ctx, getVendorBankAccountByID, id)
	var i VendorBankAccount
	err := row.Scan(
		&i.ID,
		&i.VendorID,
		&i.BankName,
		&i.AccountNumber,
		&i.AccountHolder,
		&i.BankBranch,
		&i.IsPrimary,
		&i.Note,
		&i.CreatedAt,
	)
	return &i, err
}

const listAllVendorBankAccounts = `-- name: ListAllVendorBankAccounts :many
SELECT id, vendor_id, bank_name, account_number, account_holder, bank_branch,
       is_primary, note, created_at
FROM vendor_bank_accounts
ORDER BY vendor_id, is_primary DESC, bank_name, created_at
`

func (q *Queries) ListAllVendorBankAccounts(ctx context.Context) ([]*VendorBankAccount, error) {
	rows, err := q.db.Query(ctx, listAllVendorBankAccounts)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []*VendorBankAccount
	for rows.Next() {
		var i VendorBankAccount
		if err := rows.Scan(
			&i.ID,
			&i.VendorID,
			&i.BankName,
			&i.AccountNumber,
			&i.AccountHolder,
			&i.BankBranch,
			&i.IsPrimary,
			&i.Note,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listVendorBankAccounts = `-- name: ListVendorBankAccounts :many
SELECT id, vendor_id, bank_name, account_number, account_holder, bank_branch,
       is_primary, note, created_at
FROM vendor_bank_accounts
WHERE vendor_id = $1
ORDER BY is_primary DESC, bank_name, created_at
`

func (q *Queries) ListVendorBankAccounts(ctx context.Context, vendorID pgtype.UUID) ([]*VendorBankAccount, error) {
	rows, err := q.db.Query(ctx, listVendorBankAccounts, vendorID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []*VendorBankAccount
	for rows.Next() {
		var i VendorBankAccount
		if err := rows.Scan(
			&i.ID,
			&i.VendorID,
			&i.BankName,
			&i.AccountNumber,
			&i.AccountHolder,
			&i.BankBranch,
			&i.IsPrimary,
			&i.Note,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, &i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const promoteOldestVendorBankAccount = `-- name: PromoteOldestVendorBankAccount :exec
UPDATE vendor_bank_accounts SET is_primary = true
WHERE id = (
    SELECT id FROM vendor_bank_accounts
    WHERE vendor_id = $1
    ORDER BY created_at
    LIMIT 1
)
AND NOT EXISTS (
    SELECT 1 FROM vendor_bank_accounts WHERE vendor_id = $1 AND is_primary
)
`

func (q *Queries) PromoteOldestVendorBankAccount(ctx context.Context, vendorID pgtype.UUID) error {
	_, err := q.db.Exec(ctx, promoteOldestVendorBankAccount, vendorID)
	return err
}

const updateVendorBankAccount = `-- name: UpdateVendorBankAccount :one
UPDATE vendor_bank_accounts
SET bank_name = $1, account_number = $2, account_holder = $3, bank_branch = $4,
    is_primary = $5, note = $6
WHERE id = $7
RETURNING id, vendor_id, bank_name, account_number, account_holder, bank_branch,
          is_primary, note, created_at
`

type UpdateVendorBankAccountParams struct {
	BankName      string      `json:"bank_name"`
	AccountNumber string      `json:"account_number"`
	AccountHolder string      `json:"account_holder"`
	BankBranch    string      `json:"bank_branch"`
	IsPrimary     bool        `json:"is_primary"`
	Note          string      `json:"note"`
	ID            pgtype.UUID `json:"id"`
}

func (q *Queries) UpdateVendorBankAccount(ctx context.Context, arg *UpdateVendorBankAccountParams) (*VendorBankAccount, error) {
	row := q.db.QueryRow(ctx, updateVendorBankAccount,
		arg.BankName,
		arg.AccountNumber,
		arg.AccountHolder,
		arg.BankBranch,
		arg.IsPrimary,
		arg.Note,
		arg.ID,
	)
	var i VendorBankAccount
	err := row.Scan(
		&i.ID,
		&i.VendorID,
		&i.BankName,
		&i.AccountNumber,
		&i.AccountHolder,
		&i.BankBranch,
		&i.IsPrimary,
		&i.Note,
		&i.CreatedAt,
	)
	return &i, err
}
