-- Irreversible: the original mixed casing of each row is not recoverable once
-- folded to lower_snake_case. The normalized values remain valid and readable,
-- so the down migration intentionally does nothing.
SELECT 1;
