version: "2"
sql:
  - engine: "postgresql"
    queries: "queries/"
    schema: "migrations/"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_json_tags: true
        emit_params_struct_pointers: true
        emit_result_struct_pointers: true
        sql_package: "pgx/v5"
